overrides:
  # Transitive dep of next; next pins a vulnerable range, so patch it here.
  # GHSA-qx2v-qp2m-jg93 - XSS via unescaped </style> in CSS stringify output.
  # Drop this once next ships postcss >=8.5.10.
  postcss@<8.5.10: '>=8.5.10'
